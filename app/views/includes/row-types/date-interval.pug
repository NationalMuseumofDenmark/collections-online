include ../../mixins/format-date-interval

tr
  th #{row.title}:
  td
    - var fromDate = helpers.getDate(metadata, row.fields.from, row.format);
    - var toDate = helpers.getDate(metadata, row.fields.to, row.format);
    +formatDateInterval(fromDate, toDate)
