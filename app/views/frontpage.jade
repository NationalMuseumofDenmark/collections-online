extends index


block content
  .container-fluid
    .row
      each catalog in catalogs
        if catalog
          .col-sm-4= catalog.name
            img(src=catalog.sources.imageSet[400].src)

  include includes/footer
